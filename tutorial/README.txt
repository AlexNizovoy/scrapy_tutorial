Using crawler for dx.com's search string:
scrapy crawl dx -o <outfile.json> -a s=<search string>

Examples:
scrapy crawl dx -o FL.json -a s=flashlight


scrapy crawl dx -o arduino.json -a s="arduino power"
